import { Typography } from '@mui/material';

export const AdditionalFilter = () => {
  return (
    <div>
      <Typography sx={{ color: 'primary.main', fontWeight: 600 }}>Lọc theo tổ bộ môn</Typography>
    </div>
  );
};
